/**
 * @license
 * Copyright 2023 Google LLC
 * SPDX-License-Identifier: Apache-2.0
 */
import { GraphDescriptor, NodeDescriptor, Edge, Kit, InputValues as OriginalInputValues, OutputValues as OriginalOutputValues, BreadboardRunner, BreadboardRunResult, NodeHandlerContext, BreadboardValidator } from "../../types.js";
/**
 * Implements the current API, so that we can run in existing Breadboard
 * environments.
 */
export declare class Runner implements BreadboardRunner {
    #private;
    kits: Kit[];
    edges: Edge[];
    nodes: NodeDescriptor[];
    args?: OriginalInputValues;
    constructor();
    run({ probe, kits, }: NodeHandlerContext): AsyncGenerator<BreadboardRunResult>;
    runOnce(inputs: OriginalInputValues, context?: NodeHandlerContext): Promise<OriginalOutputValues>;
    addValidator(_: BreadboardValidator): void;
    static fromGraphDescriptor(graph: GraphDescriptor): Promise<Runner>;
    static load(url: string, options: {
        base: URL;
        outerGraph?: GraphDescriptor;
    }): Promise<Runner>;
}
//# sourceMappingURL=runner.d.ts.map