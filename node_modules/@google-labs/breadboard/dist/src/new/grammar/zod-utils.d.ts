/**
 * @license
 * Copyright 2023 Google LLC
 * SPDX-License-Identifier: Apache-2.0
 */
import { z } from "zod";
import { Schema } from "../../types.js";
import { NodeFactory } from "./types.js";
import { InputValues, OutputValues } from "../runner/types.js";
/**
 * This post processed JSON schema generated from Zod:
 *  - adds a title to the schema or any field by parsing the description as
 *    `${title}: ${description}`
 *  - removes $schema field
 *
 * @param zod Zod schema
 * @returns Post processed `Schema` object
 */
export declare function zodToSchema(zod: z.ZodType<unknown> | Schema): Schema;
export declare function convertZodToSchemaInConfig<I extends InputValues, O extends OutputValues>(config: {
    schema?: z.ZodType | Schema;
    $id?: string;
}, factory: NodeFactory<I, O>): import("./types.js").NodeProxy<I, O>;
//# sourceMappingURL=zod-utils.d.ts.map