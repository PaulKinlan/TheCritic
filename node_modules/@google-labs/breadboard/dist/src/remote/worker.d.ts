/**
 * @license
 * Copyright 2023 Google LLC
 * SPDX-License-Identifier: Apache-2.0
 */
import { PatchedReadableStream, PortStreams } from "../stream.js";
import { ClientTransport, ServerBidirectionalStream, ServerTransport } from "./protocol.js";
export declare class PortDispatcher {
    #private;
    constructor(worker: Worker);
    receive<Request, Response>(id: string): PortStreams<Request, Response>;
    send<Request, Response>(id: string): PortStreams<Request, Response>;
}
export declare class WorkerClientTransport<Request, Response> implements ClientTransport<Request, Response> {
    #private;
    constructor(streams: PortStreams<Response, Request>);
    createClientStream(): {
        writableRequests: WritableStream<Request>;
        readableResponses: PatchedReadableStream<Response>;
    };
}
export declare class WorkerServerTransport<Request, Response> implements ServerTransport<Request, Response> {
    #private;
    constructor(streams: PortStreams<Request, Response>);
    createServerStream(): ServerBidirectionalStream<Request, Response>;
}
//# sourceMappingURL=worker.d.ts.map